# Rust
/target

# Python
/.venv
/dist
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.ruff_cache/

# Data / generated files
/data

# protoc output for examples/bench/transport/record.proto (generated on first use; see
# examples/bench/transport/zerobus.py)
/examples/bench/transport/record_pb2.py

# Local Lakebase/Zerobus connection settings filled in from connection.example.yaml
# (may contain real endpoint/profile names -- never commit a filled-in copy)
/examples/bench/connection.yaml

# Local Lakebase/Zerobus credentials filled in from .env.example (may contain a real
# PAT/client secret -- never commit a filled-in copy)
/examples/bench/.env

# Rotated Zerobus service-principal secrets, cached in plaintext between rotations (see
# _SECRET_CACHE_DIR in examples/bench/transport/zerobus.py) -- never commit these
/examples/bench/.cache/

# IDE / tooling
/.claude
/.cursor
/.databricks
